<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             Flutter draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
        <!-- **ويُضبطان هنا أيضاً** — لحظةُ الإقلاع تسبق أوّلَ إطارٍ من
             فلاتر، فبلا ضبطٍ هنا يرى المستخدمُ الشريطين أسودين ثمّ
             يتغيّران: وميضٌ يُقرأ عطلاً. -->
        <item name="android:statusBarColor">@color/amial_status_bar</item>
        <item name="android:navigationBarColor">@color/amial_nav_bar</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
        <!-- AMIAL-INSETS-001 — بلا هذين يبقى الشريطان أسودَي الثيم الأصل. -->
        <item name="android:statusBarColor">@color/amial_status_bar</item>
        <item name="android:navigationBarColor">@color/amial_nav_bar</item>
        <!-- شريطُ التنقّل فاتح ⇒ أيقوناتُه داكنة، وإلّا اختفت. -->
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>