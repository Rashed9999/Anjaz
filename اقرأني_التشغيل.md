# أميال باي — النسخة الكاملة القابلة للتشغيل (v2.55, مع قاعدة البيانات)

```
amial_pay_FULL/
├── 00_database/amial_pay_database.sql   ← قاعدة البيانات
├── 01_backend/  ·  02_flutter_app/
└── DEEP_CHECK_REPORT.md · PRODUCTION_READINESS.md · TESTING.md
```
> مُستبعَد (يُعاد توليده): vendor/ (composer install)، build|.dart_tool (flutter pub get)، و.env.

## التشغيل
```bash
cd 01_backend && composer install
cp .env.example .env && php artisan key:generate
mysql -u<USER> -p amial_pay < ../00_database/amial_pay_database.sql
php artisan passport:keys --force
php -r "echo 'AMIAL_PII_ENCRYPTION_KEY='.base64_encode(random_bytes(32)).PHP_EOL;" >> .env
php -r "echo 'AMIAL_PII_BLIND_INDEX_KEY='.base64_encode(random_bytes(32)).PHP_EOL;" >> .env
php artisan serve && php artisan test

cd 02_flutter_app && flutter pub get && flutter test
flutter run --dart-define=BASE_URL=https://api.your-domain.com   # أو -d chrome للوحة الويب
```

## الجديد في v2.55 — قسم «التشغيل» في مركز الإعدادات
كل الإدارة التشغيلية صارت أزراراً في لوحة أدمن التطبيق (بلا باكند):
- **وضع الصيانة**: مفتاح + رسالة — يحجب المستخدمين بـ 503 ويُبقي الأدمن والدخول وping.
- **فحص صحّة النظام**: زرّ يعرض حالة (قاعدة البيانات/الكاش/التخزين/الطوابير/القرص/PHP).
- **تنظيف الكاش**: زرّ واحد.
- **فرض تحديث التطبيق**: أدنى/أحدث إصدار + رسالة، مع endpoint عام `GET /api/v1/amial/app-version`.

> سابقاً: مركز الإعدادات (واتساب/SMS/رسوم/تواصل/إشعارات)، باركود الكاشير، لوحة الوكيل،
> الفحص العميق (673+ اختباراً)، WhatsApp OTP، إكمال هرم الاختبارات.

## الترخيص
الخلفية فوق قاعدة 6cash/Cash6 (CodeCanyon) — لك بصفتك مالك الترخيص؛ لا تُعِد توزيع القاعدة.
