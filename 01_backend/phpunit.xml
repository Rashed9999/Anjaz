<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
    </testsuites>
    <coverage processUncoveredFiles="true">
        <include>
            <directory suffix=".php">./app</directory>
        </include>
    </coverage>
    <php>
        <!-- AMIAL-GATE-BLINDSPOT-001 — **حدُّ الذاكرة يُوضع هنا لا في سطر
             الأمر.** `artisan test` يشغّل phpunit في عمليّةٍ ابنة، فـ
             `php -d memory_limit=...` يصيب الأبَ وحدَه والابنُ يبقى على
             ٥١٢ ميغابايت. وقِيس: المجموعةُ تنهار عند ٥١٢، ولم يكن يُطبع
             سطرُ نتيجةٍ إطلاقاً، فتقرأ البوّابةُ الغيابَ نجاحاً. -->
        <ini name="memory_limit" value="2048M"/>
        <server name="APP_ENV" value="testing"/>
        <server name="BCRYPT_ROUNDS" value="4"/>
        <server name="CACHE_DRIVER" value="array"/>
        <!-- <server name="DB_CONNECTION" value="sqlite"/> -->
        <!-- <server name="DB_DATABASE" value=":memory:"/> -->
        <server name="MAIL_MAILER" value="array"/>
        <server name="QUEUE_CONNECTION" value="sync"/>
        <server name="SESSION_DRIVER" value="array"/>
        <server name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
