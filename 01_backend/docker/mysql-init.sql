-- MySQL Init Script — يُنفَّذ تلقائياً عند أوّل تشغيل الحاوية
-- (لا تعديل مطلوب منك)

SET NAMES utf8mb4;
SET time_zone = '+03:00';  -- توقيت اليمن (UTC+3)

ALTER DATABASE amial_pay CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
