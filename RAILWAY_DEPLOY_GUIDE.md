# دليل رفع الباكند على Railway — خطوة بخطوة (لغير المبرمجين)

> **لماذا فشل البناء عندك؟** لأن مستودعك يحوي 3 مجلّدات (`00_database`، `01_backend`، `02_flutter_app`)، وRailway كان يبحث عن الباكند في **جذر المستودع** بينما هو داخل `01_backend`. فشل خلال 3 ثوانٍ لأنه لم يجد ملف البناء. الحل: نُخبر Railway بمكان الباكند بالضبط. أضفتُ ملفات الإعداد اللازمة، وبقيت **3 إعدادات في لوحة Railway** تضبطها بنفسك (لا يمكن ضبطها من الكود).

---

## الخطوة 1 — حدّد مجلّد الباكند (الأهم — يحلّ فشل البناء)

في Railway، افتح خدمتك (Service) ثم:
**Settings → Source → Root Directory** → اكتب:
```
01_backend
```
احفظ. هذا يجعل Railway يبني من مجلّد الباكند مباشرةً (حيث Dockerfile).

---

## الخطوة 2 — أضِف قاعدة بيانات MySQL

الباكند يحتاج قاعدة بيانات. في مشروعك على Railway:
**+ New → Database → Add MySQL**

سيُنشئ Railway قاعدة MySQL ويولّد بياناتها تلقائياً.

---

## الخطوة 3 — اربط متغيّرات البيئة

في خدمة الباكند: **Variables → أضِف المتغيّرات التالية**.
استخدم صيغة المرجع `${{MySQL.VAR}}` لربطها بقاعدة البيانات تلقائياً:

| المتغيّر | القيمة |
|---|---|
| `DB_CONNECTION` | `mysql` |
| `DB_HOST` | `${{MySQL.MYSQLHOST}}` |
| `DB_PORT` | `${{MySQL.MYSQLPORT}}` |
| `DB_DATABASE` | `${{MySQL.MYSQLDATABASE}}` |
| `DB_USERNAME` | `${{MySQL.MYSQLUSER}}` |
| `DB_PASSWORD` | `${{MySQL.MYSQLPASSWORD}}` |
| `APP_ENV` | `production` |
| `APP_DEBUG` | `false` |
| `APP_KEY` | *(انظر أسفله)* |
| `RUN_SEEDERS` | `true` *(أول مرة فقط، لتعبئة بيانات تجريبية)* |

> **APP_KEY:** مفتاح تشفير إجباري. ولّده مرّة واحدة: افتح موقع
> `https://generate.plus/en/base64?bytes=32` وانسخ الناتج، ثم ضعه بصيغة:
> `base64:الناتج_هنا`
> (أو اتركه فارغاً وسيُولَّد مؤقتاً، لكن يُفضّل تثبيته حتى لا تتعطّل الجلسات عند كل نشر.)

---

## الخطوة 4 — أعِد النشر

اضغط **Deploy** (أو ادفع أي تعديل — سيُعاد النشر تلقائياً).

**ماذا يحدث تلقائياً عند النشر (لا تفعل شيئاً):**
- Railway يبني صورة Docker من `01_backend/Dockerfile`.
- عند الإقلاع: يضبط المنفذ تلقائياً، ينتظر قاعدة البيانات، يُطبّق كل الجداول (migrations)، ويملأ البيانات التجريبية (إن ضبطت `RUN_SEEDERS=true`).
- يبدأ الخادم، وRailway يفحص صحّته عبر `/health/liveness`.

---

## الخطوة 5 — تحقّق أنه يعمل

بعد نجاح النشر، افتح رابط خدمتك من Railway (**Settings → Networking → Generate Domain** لإنشاء رابط عام)، ثم افتح:
```
https://رابطك.up.railway.app/health/liveness
```
يجب أن ترى: `{"status":"alive",...}` ✓ — الباكند يعمل.

---

## الخطوة 6 — اربط التطبيق بالباكند

في تطبيق Flutter، عدّل عنوان الـ API ليشير إلى رابط Railway (بدل localhost).
ابحث في `02_flutter_app/lib` عن ملف الإعدادات (عادةً `app_constants.dart` أو `api_client.dart`) وضع رابط Railway العام. **أخبرني وسأضبطه لك بالضبط.**

---

## إن فشل النشر مجدداً

| العرض | السبب المحتمل | الحل |
|---|---|---|
| فشل خلال ثوانٍ في البناء | Root Directory غير مضبوط | الخطوة 1 |
| "unhealthy" / لا يستجيب | متغيّرات DB ناقصة | الخطوة 3 |
| صفحة خطأ 500 | `APP_KEY` غير مضبوط | الخطوة 3 (APP_KEY) |
| يعلّق طويلاً ثم يفشل | لم تُضف قاعدة MySQL | الخطوة 2 |

**أرسل لي صورة سجلّ الأخطاء (Build/Deploy Logs) وسأحدّد السبب فوراً.**

---

### ما الذي أضفتُه في الكود (تلقائي — لا تحتاج لمسه)
- `01_backend/railway.json`: يخبر Railway أن يبني عبر Dockerfile ويفحص الصحّة على `/health/liveness`.
- `01_backend/docker/entrypoint.sh`: يضبط منفذ Railway الديناميكي، ينتظر قاعدة البيانات بمهلة (لا تعليق لانهائي)، يطبّق الجداول، ثم يشغّل الخادم.
- `01_backend/Dockerfile`: يستدعي التهيئة أعلاه عند الإقلاع.
