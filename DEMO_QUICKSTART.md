# نشر أميال باي تجريبياً — دليل سريع (بلا خبرة برمجية)

المشروع **جاهز للتشغيل التجريبي بأمر واحد** عبر Docker — مجاناً على جهازك أو أي خادم
مجاني. لا حاجة لتثبيت PHP/MySQL يدوياً؛ Docker يجهّز كل شيء ويزرع بيانات عرض جاهزة.

## المتطلّب الوحيد
تثبيت **Docker Desktop** (مجّاني): https://www.docker.com/products/docker-desktop

## التشغيل (أمر واحد)

```bash
cd 01_backend
docker compose up -d --build
```

انتظر ~2–3 دقائق (أوّل مرّة فقط: يبني الصورة، يهاجر القاعدة، يزرع بيانات العرض).
ثمّ افتح المتصفّح على:

- **لوحة الإدارة:** http://localhost:8000/admin/auth/login
- **الـAPI (للتطبيق):** http://localhost:8000/api/v1/…

## حسابات العرض الجاهزة (مزروعة تلقائياً)

| الدور | الدخول | كلمة السرّ / PIN | الرصيد |
|---|---|---|---|
| **أدمن** (لوحة الويب — الدخول بالجوال لا الإيميل) | هاتف `967777000001` | `Pass@2026` | — |
| عميل 1 | هاتف `967777100001` | PIN `1234` | 50,000 |
| عميل 2 | هاتف `967777100002` | PIN `1234` | 25,000 |
| تاجر | هاتف `967777200001` | PIN `1234` | 200,000 |
| وكيل | هاتف `967777300001` | PIN `1234` | 500,000 |

> **للمستثمر:** ابدأ بلوحة الإدارة (أعلاه) — هي أوضح عرض؛ تُظهر التسويات، التجّار،
> الوكلاء، التقارير، إعدادات الرسوم.

## رابط عامّ للتجربة (اختياري — لعرضه لمستثمر عن بُعد)

لتحويل التشغيل المحلّي إلى **رابط عامّ على الإنترنت** (بلا خادم):

1. أنشئ حساباً مجّانياً على https://ngrok.com واحصل على `NGROK_AUTHTOKEN`.
2. أضِفه في ملفّ `01_backend/.env.demo`:
   ```
   NGROK_AUTHTOKEN=توكنك_هنا
   ```
3. شغّل مع النفق:
   ```bash
   docker compose --profile whatsapp up -d
   ```
4. افتح http://localhost:4040 → ستجد رابطاً عامّاً مثل `https://xxxx.ngrok-free.app`
   أرسِله للمستثمر ليجرّب من أي مكان.

## إيقاف / إعادة التشغيل

```bash
docker compose down          # إيقاف (تبقى البيانات)
docker compose down -v       # إيقاف + مسح البيانات (بداية نظيفة)
docker compose up -d         # إعادة تشغيل
```

## ملاحظات صدق (قبل عرض التطبيق الجوّال)

- **لوحة الإدارة (الويب) جاهزة للعرض فوراً** بحساب الأدمن أعلاه.
- **دخول العميل/الوكيل/التاجر عبر التطبيق الجوّال** يحتاج توحيد التسجيل/الدخول
  (موثّق في `AMIAL_STRESS_TESTS_RESULTS.md` — قسم رحلة المستخدم): الدخول الموحّد
  يطلب `national_id` ولا يجمعه التسجيل بعد. يُنصَح بحلّه قبل عرض التطبيق الجوّال
  للمستثمر (تعديل بسيط في التسجيل/الدخول).
- بيانات العرض للتجربة فقط — لا تستخدمها في الإنتاج.
