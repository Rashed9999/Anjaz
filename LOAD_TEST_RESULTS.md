# نتائج اختبار التحمّل/التزامن — هل يثبت النظام أم ينهار؟

تشغيل حقيقي: 6cash + أميال مدموجان على **MariaDB 10.11 + Laravel 12.44 + PHP 8.4**.

## السؤال
> 5000 مستخدم نشط في نفس اللحظة ينفّذون عملياتهم — هل يثبت النظام أم ينهار؟

## المنهج (صادق)
5000 مستخدم HTTP متزامن **فعلياً** يتطلّب بنية staging (php-fpm/nginx + مولّد حمل
موزّع) — ولهذا تُوجد سكربتات k6 (`loadtests/staged_all_features.js`). داخل بيئة
الفحص أجرينا ما هو **أكثر حسماً للسؤال**: اختبار تزامن حقيقي على **القلب المالي**
— عشرات العمليات المتوازية (`lockForUpdate` فعلي) تنفّذ **5000 تحويلاً** على محافظ
مشتركة، وقياس: الإنتاجية، الـ deadlocks، الأخطاء، و**سلامة الأموال** (الأهم).

الأداة: `01_backend/loadtests/concurrency/` (seed + worker + run.sh).

## النتائج

| التزامن (عمليات متوازية) | العمليات | نجاح | deadlock | أخطاء | الزمن | الإنتاجية | سلامة المال |
|---|---|---|---|---|---|---|---|
| 50  | 5000 | 5000 | **0** | 0 | 32.5s | ~154 tx/s | ✅ محفوظ بالضبط |
| 100 | 5000 | 5000 | **0** | 0 | 36.9s | ~136 tx/s | ✅ محفوظ بالضبط |
| 200 | 5000 | 3800 | **0** | 1200¹ | 38.5s | ~99 tx/s | ✅ محفوظ بالضبط |

¹ أخطاء الـ200 كلها **`Too many connections` (SQLSTATE 1040)** — حدّ MariaDB الافتراضي
`max_connections=151`. **ليست deadlocks ولا فساد بيانات**: الاتصال الزائد يُرفض قبل
بدء المعاملة، فلا يُلمَس أي مال.

### فحص سلامة الأموال (الحاسم)
في كل المستويات: `مجموع المحافظ + مجموع رسوم المنصّة = 100,000,000` بالضبط (الرصيد
الابتدائي) — أي **صفر تسرّب وصفر تضاعف** رغم التزامن الشديد. (مثال 50×: محافظ
99,997,500 + رسوم 2,500 = 100,000,000؛ و2,500 = 5000 × 0.5 رسوم.)

## الحُكم: ✅ النظام يثبت — لا ينهار

- **حتى ~150 عملية متزامنة** (= حدّ اتصالات MariaDB الافتراضي): **نجاح 100%، صفر
  deadlock، أموال دقيقة**. أثبت هذا صحّة `lockWalletsOrdered` + `lockForUpdate` +
  `version` + حساب `MoneyService` (bcmath).
- **فوق الحدّ:** تدهور **رشيق** (graceful) — يرفض الفائض بنظافة، **لا انهيار ولا
  فساد مال**.
- العنق الوحيد = `max_connections` (إعداد بنية تحتية، لا خلل كود).

## للوصول لـ 5000 متزامن حقيقي في الإنتاج
1. `max_connections` ≥ المتوقّع (مثلاً 1000–4000) + `innodb_buffer_pool_size` مناسب.
2. php-fpm (عشرات/مئات الـ workers) خلف nginx + موازِن أحمال (عدّة خوادم تطبيق).
3. تجميع اتصالات DB (ProxySQL/PgBouncer-style) أو خادم DB أقوى/replicas للقراءة.
4. الإشعارات async عبر `queue:work` منفصل (مُطبّق — تُرتَّل في `jobs`).
5. ثم شغّل `loadtests/staged_all_features.js` (تدرّج 20→10000 + دفعة) على staging
   مع `loadtests/concurrency/run.sh` للتحقّق من سلامة المال تحت الضغط.

## إعادة التشغيل
```bash
cd 01_backend
./loadtests/concurrency/run.sh 50 100 100     # 50 عاملاً × 100 = 5000 عملية
./loadtests/concurrency/run.sh 100 50 100     # تزامن أعلى
```

---

# اختبار ضغط الكاشير — 5000 فاتورة بيع متزامنة

`recordSale` (بيع نقدي): إدراج فاتورة في `merchant_sales` + **خصم مخزون بقفل صف
المنتج** (`lockForUpdate`، يمنع المخزون السالب). الأداة:
`loadtests/concurrency/{seed_cashier.php, cashier_worker.php, run_cashier.sh}`.

| الوضع | التزامن | فواتير ناجحة | deadlock | أخطاء | الزمن | الإنتاجية | سلامة المخزون |
|---|---|---|---|---|---|---|---|
| spread (20 تاجر) | 50  | 5000 | **0** | 0 | 7.5s  | **663 فاتورة/s** | ✅ مطابق |
| hot (تاجر/منتج واحد) | 50  | 5000 | **0** | 0 | 9.3s  | **536 فاتورة/s** | ✅ مطابق |
| spread | 100 | 5000 | **0** | 0 | 14.5s | 346 فاتورة/s | ✅ مطابق |
| hot    | 200 | 3825¹ | **0** | 0 | 16.2s | 236 فاتورة/s | ✅ مطابق (بلا فساد) |

¹ الفائض عن حدّ `max_connections=151` لم يتّصل أصلاً (لم يُنفَّذ بيعاً) — لا فساد.

## سلامة المخزون (الحاسم)
في كل المستويات: `المخزون المخصوم = عدد الفواتير الناجحة` بالضبط، وعدد صفوف
`merchant_sales` = الناجحة. **حتى في وضع hot** (5000 فاتورة تتزاحم على **صف منتج
واحد**): صفر deadlock، صفر بيع زائد (oversell)، صفر مخزون سالب. أثبت `lockForUpdate`
التسلسل الصحيح تحت أقصى تزاحم.

## الحُكم: ✅ الكاشير يثبت بقوة
- الكاشير **أسرع** من التحويل (663 مقابل ~154 فاتورة/s) لأنه إدراج + قفل صف واحد
  (لا أقفال محافظ متعدّدة ولا رسوم).
- حتى التزاحم على **منتج واحد** (أسوأ حالة) يثبت بلا deadlock ولا فساد مخزون.
- نفس العنق: `max_connections` (إعداد بنية تحتية).

## إعادة التشغيل
```bash
cd 01_backend
./loadtests/concurrency/run_cashier.sh 50 100 spread 20   # 5000 فاتورة موزّعة
./loadtests/concurrency/run_cashier.sh 50 100 hot 20      # 5000 على منتج واحد (أقصى تزاحم)
```
