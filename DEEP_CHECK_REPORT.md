# تقرير الفحص العميق — جاهزية التشغيل (v2.53)

تاريخ الفحص: 2026-06-10 — تشغيل حقيقي على MariaDB 10.11 + Laravel 12.44 + PHP 8.4 + Flutter 3.44.

## ✅ النتيجة الإجمالية: المشروع يعمل بلا أخطاء

| الفحص | النتيجة |
|---|---|
| **اختبارات الخلفية الكاملة** | ✅ **673 / 673 ناجح** (1932 تأكيداً، ~3.6 دقيقة) |
| اختبارات Flutter | ✅ **12 / 12 ناجح** |
| `flutter analyze` | ✅ 0 أخطاء، 0 تحذيرات (42 ملاحظة info موثّقة) |
| المسارات (`route:list`) | ✅ **733 مساراً** تُحمَّل بلا أخطاء (كانت تنهار — انظر الإصلاحات) |
| إقلاع التطبيق (`artisan about`) | ✅ Laravel 12.44 / PHP 8.4.19 |
| `composer validate` | ✅ صالح (تحذيران تجميليان لقيود إصدارات من قاعدة 6cash) |
| E2E على جهاز حقيقي | ✅ (سابقاً) إقلاع كامل + أوّل نداء API |
| تحمّل/تزامن | ✅ (سابقاً) 0 deadlock، أموال/مخزون محفوظة بالضبط |

## 🐞 أخطاء حقيقية اكتُشفت وأُصلحت أثناء هذا الفحص

**فئة خلل في 6 متحكّمات بوابات دفع من قاعدة 6cash** — إذا وُجد صفّ إعداد للبوابة
بقيم JSON فارغة أو وضع غير متوقّع، ينهار الـ constructor بـ
`Undefined variable $values` / `null dereference`، ما كان:
1. يُسقط أي طلب يلمس تلك البوابة، **و**
2. يكسر أوامر تشخيصية كاملة مثل `php artisan route:list`.

أُصلحت دفاعياً (حراسة القيم قبل القراءة) في:
`SslCommerz`, `Bkash`, `CashFree`, `Instamojo`, `Paypal`, `Phonepe`.
بقية البوابات (Stripe/Paystack/Flutterwave/SenangPay/Paymob/MercadoPago/RazorPay)
فُحصت — آمنة بنيوياً.

## 🆕 مركز الإعدادات الموحّد (AMIAL-SETTINGS-CENTER-001)
كل ما كان يتطلّب الدخول للباكند صار يُدار من **شاشة «مركز الإعدادات»** في لوحة
أدمن التطبيق (زرّ ⚙️):

| القسم | ما يُدار |
|---|---|
| واتساب | المزوّدون الخمسة + ترتيب القناة + إرسال تجريبي |
| SMS | twilio / nexmo / 2factor / msg91 (الأسرار مُقنّعة) |
| إشعارات واتساب | تفعيل + اختيار الأنواع |
| بيانات التواصل | رقم واتساب/هاتف/إيميل الدعم (+ endpoint عام للتطبيق) |
| الرسوم ونسب الأرباح | عرض النشِط، إنشاء نسخة، **محاكاة قبل الحفظ**، تعطيل |

- خلفية: `AdminSettingsController` (10 endpoints محمية أدمن) + endpoint عام
  `GET /api/v1/amial/support-contact`.
- اختبارات: `AdminSettingsCenterTest` (5 اختبارات/43 تأكيداً) + اختبار واجهة
  للشاشة ضمن `screens_widget_test`.

## المتبقّي قبل الإطلاق (تشغيلي، ليس برمجياً)
انظر `PRODUCTION_READINESS.md`: مفاتيح حيّة (دفع/SMS/واتساب — **تُدخَل الآن من
مركز الإعدادات**)، HTTPS، نسخ احتياطي، رفع `max_connections`، مراقبة.
