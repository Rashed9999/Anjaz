# AMIAL-WA-003 — دفع الفواتير + طلبات الدفع عبر رابط/رمز/QR (الجولة 2)

تنفيذاً لـ Section 22 (دفع الفواتير) و Section 23 (QR عبر واتساب) من
مواصفة AMYAL PAY WHATSAPP BOT SPECIFICATION v1.0.

## ✅ ما تمّ بناؤه

### Section 22 — دفع الفواتير
- تدفّق كامل: اختيار خدمة → رقم الاشتراك → استعلام حقيقي (inquire) →
  فاتورة آجلة بمبلغ معروف تتخطّى خطوة إدخال المبلغ تلقائياً، أو خدمة
  مسبقة الدفع تطلب المبلغ من المستخدم.
- يُعاد استخدام `BillPayService::createAndExecute()` الموجود بالكامل —
  لا منطق مالي جديد مطلقاً. إضافة وحيدة: `previewFee()` (wrapper عام
  حول دالّة خاصّة موجودة، لإظهار الرسوم في رسالة التأكيد فقط).
- القوائم مُصفّاة حسب `zone_code` المستخدم تلقائياً — لا قائمة عالمية
  ثابتة، ولا تكرار لمنطق Zone Policy الموجود.

### Section 23 — طلبات الدفع عبر رابط/رمز/QR
- نظام `PaymentRequest` الموجود مسبقاً (`short_code` + `publicUrl()`)
  أُستُخدم كما هو — لا نظام جديد.
- صورة QR عبر واتساب: تنزيل (`WhatsappMediaDownloader` عبر Meta Graph
  API) ← فكّ شيفرة (`WhatsappQrDecoderService` عبر مكتبة
  `khanamiryan/qrcode-detector-decoder`، PHP خالص + GD الموجود مسبقاً)
  ← النصّ المفكوك يُعامَل كرسالة نصّية عادية (لا منطق مكرّر).
- النصّ المباشر (رمز 6 خانات أو رابط `/req/{code}`) يعمل بالتساوي عبر
  نفس `IntentParser::extractPaymentRequestCode()` — مصدر وحيد للمنطق.

## ⚠️ مطلوب منك بعد الدمج

```bash
composer update    # لإضافة khanamiryan/qrcode-detector-decoder الجديدة
php artisan test --filter=WhatsappBillPayAndPaymentRequestTest   # 25 اختبار
```

## صدق هندسي — حدود معروفة

- **اختبارات الفواتير** تُموِّه `BillPayService::createAndExecute()`
  عمداً عند نقطة التنفيذ النهائي فقط، لأنّ `StubProvider::pay()`
  الحقيقي عشوائي (`crc32(...) % 100`) — اختباره بصدقية كاملة هناك
  سيكون flaky. باقي التدفّق (الاستعلام، اختيار الخدمة، التحقّقات
  المالية) **حقيقي 100%** عبر `StubProvider::inquire()` الحتمي.
- **اختبارات طلبات الدفع** حقيقية بالكامل بلا أيّ Mock — تتحقّق من
  الأرصدة الفعلية في `e_money` قبل/بعد، لا فقط من رسائل النجاح.
- **فكّ صورة QR حقيقية** (بكسلات فعلية) لم تُختبَر بصورة QR مُولَّدة
  فعلياً ضمن الاختبارات الآلية (يتطلّب توليد صورة QR حقيقية في وقت
  الاختبار، تكلفة/تعقيد غير مبرَّر لمنطق بحجم decode() البسيط). الدالّة
  مُختبَرة على معالجة الفشل (بايتات غير صالحة) فقط. يُنصَح باختبار يدوي
  واحد بصورة QR حقيقية بعد الرفع للإنتاج.
- **التحقّق من PIN**: `createAndExecute()` و `PaymentRequestService::pay()`
  لا يتحقّقان من PIN داخلياً (بعكس التحويل) — أضفتُ تحقّقاً صريحاً عبر
  `TransactionPinService::verify()` قبل استدعائهما، بمراكمة Risk Score
  عند الفشل (نفس نمط Section 26 من الجولة السابقة).

## الجولات المتبقّية

| # | المحتوى |
|---|---|
| 3 | أوامر التاجر: مبيعات اليوم/الشهر، رصيد، إنشاء فاتورة (Section 15-16) |
| 4 | أوامر الوكيل: سيولة، إيداع، سحب (Section 17-18) |
| 5 | أوامر الإدارة + Support Ticket كامل + AML integration (Section 19, 25) |
