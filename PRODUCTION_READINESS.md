# دليل جاهزية الإنتاج — أميال باي

قائمة تدقيق عملية قبل إطلاق المشروع للإنتاج. مقسّمة حسب الأولوية.
الحالة: ✅ جاهز · ⚠️ يحتاج إعداد · ⛔ حاجب (يجب قبل الإطلاق).

---

## 1) الأسرار والمفاتيح (⛔ حاجب)
| البند | الحالة | الإجراء |
|---|---|---|
| `APP_KEY` | ⚠️ | `php artisan key:generate` |
| مفاتيح Passport (OAuth) | ⚠️ | `php artisan passport:keys --force` ثم أنشئ عميل: `passport:client --personal` |
| `AMIAL_PII_ENCRYPTION_KEY` + `AMIAL_PII_BLIND_INDEX_KEY` | ⛔ | إلزاميان للإقلاع — ولّدهما `base64(random_bytes(32))` واحفظهما في خزنة أسرار (ليس Git) |
| مفاتيح بوّابات الدفع (Stripe/Razorpay/Paystack/…) | ⚠️ | استبدل القيم الفارغة في `.env` بمفاتيح **الإنتاج** الحيّة |
| Twilio/Vonage (SMS/OTP) | ⚠️ | مفاتيح حيّة + رقم مُرسِل مُوثّق |
| `.env` خارج Git | ✅ | مُستبعَد؛ النسخة المُسلَّمة تحوي `.env.example` فقط (لا أسرار) |
| تدوير المفاتيح | ⚠️ | خطّة تدوير ربع سنوية لمفاتيح PII والـ payment |

## 2) قاعدة البيانات (⚠️)
| البند | الحالة | الإجراء |
|---|---|---|
| الهجرات (134) | ✅ | تمر كلها — `php artisan migrate --force` |
| `max_connections` | ⚠️ | الافتراضي 151؛ ارفعه لـ 1000–4000 حسب الحمل المتوقّع (انظر LOAD_TEST_RESULTS.md) |
| `innodb_buffer_pool_size` | ⚠️ | ~70% من RAM الخادم |
| النسخ الاحتياطي | ⛔ | نسخ يومي آلي + اختبار استرجاع شهري (مالية!) |
| Read replicas | ⚠️ | للقراءات الثقيلة (التقارير/اللوحات) عند الكبر |
| تشفير at-rest | ⚠️ | فعّل تشفير القرص/الـ tablespace للبيانات المالية |

## 3) الأمان (⛔/⚠️)
| البند | الحالة | الإجراء |
|---|---|---|
| HTTPS/TLS | ⛔ | شهادة صالحة + إعادة توجيه HTTP→HTTPS + HSTS |
| Certificate Pinning (التطبيق) | ⚠️ | املأ `_primaryCertFingerprintSha256` في `certificate_pinning_helper.dart` ببصمة شهادتك قبل build الإنتاج |
| Security Sentinel | ✅ | مُفعّل (كشف الأخطاء والتسلّل) — راجع SECURITY_SENTINEL.md |
| Rate limiting | ✅ | `PerUserRateLimit` مُصلَح ومُفعّل |
| Idempotency | ✅ | `EnforceIdempotency` على العمليات المالية |
| سياسة المنطقة (جنوب فقط) | ✅ | `EnforceZonePolicy` + اختبار حظر الشمال (8 حالات) |
| فحص الأسرار في CI | ✅ | Gitleaks في `.github/workflows/ci.yml` |
| `composer audit` / تبعيات | ⚠️ | راجِع تحذيرات الثغرات دورياً |

## 4) الأداء والبنية التحتية (⚠️)
| البند | الحالة | الإجراء |
|---|---|---|
| php-fpm خلف nginx | ⚠️ | عشرات/مئات الـ workers + موازِن أحمال (عدّة خوادم تطبيق) |
| `ext-bcmath` الحقيقي | ⚠️ | بيئة الفحص استخدمت polyfill؛ ثبّت الامتداد الأصلي (أدقّ/أسرع) |
| الطوابير (queues) | ⚠️ | `queue:work` مُشرف عليه بـ Supervisor للإشعارات/المهام الثقيلة |
| الكاش | ⚠️ | Redis للجلسات/الكاش بدل الملفات |
| `config:cache` + `route:cache` | ⚠️ | في النشر الإنتاجي |

## 5) المراقبة والرصد (⚠️)
| البند | الحالة | الإجراء |
|---|---|---|
| Sentry | ⚠️ | `sentry/sentry-laravel` مُضاف — اضبط `SENTRY_LARAVEL_DSN` |
| سجلّات مركزية | ⚠️ | شحن اللوقات (نظام ELK/Loki) مع تنبيهات |
| اللوحة التنفيذية | ✅ | متوفّرة (EXECUTIVE_DASHBOARD.md) |
| Health checks | ⚠️ | نقطة `/health` + مراقبة uptime |
| تنبيهات مالية | ⛔ | تنبيه فوري على فشل تسوية الـ Ledger أو اختلال الرصيد |

## 6) الاختبارات والجودة (✅ غالباً)
| البند | الحالة |
|---|---|
| اختبارات الخلفية | ✅ **615/615 ناجح** (MariaDB + Laravel 12.44 + PHP 8.4) |
| اختبار حظر الشمال | ✅ 8 حالات |
| اختبار التحمّل/التزامن | ✅ 0 deadlock، الأموال/المخزون محفوظة (LOAD_TEST_RESULTS.md) |
| Flutter analyze | ✅ **0 أخطاء، 0 تحذيرات** (خُفِّضت الملاحظات 308→42 info) |
| Flutter test (headless) | ✅ اختبارات واجهة حتمية (لغات + توجيه الأدوار) — 4/4 |
| **اختبار E2E على جهاز حقيقي** | ✅ **نجح** — التطبيق الكامل يُقلع على جهاز Linux desktop (تحت Xvfb): DI كامل + بناء الواجهة + أوّل نداء API (`GET /api/v1/config`) دون انهيار |
| اختبار حمل staging | ⚠️ شغّل `loadtests/staged_all_features.js` على بيئة قريبة من الإنتاج |

### إصلاحات صلابة اكتُشفت أثناء اختبار E2E الحقيقي (أخطاء إنتاج فعلية)
تشغيل التطبيق على جهاز حقيقي كشف **3 أخطاء** كانت تُسقط الإقلاع عند فشل/غياب إضافة
native — أُصلحت كلها بالتدهور الرشيق (تعمل على الأجهزة، وتُحصِّن ضد فشل الإضافات):
1. **`unique_identifier`** — رميه كان يُسقط `di.init()` كاملاً → محاط بـ try/catch (يسقط لمعرّف فارغ).
2. **`connectivity_plus`** — خطأ تهيئته كان يُسقط شاشة البداية → قُصِر على الموبايل + معالج `onError` + إلغاء الاشتراك في `dispose`.
3. **`local_auth` (القياسات الحيوية)** — فحص الدعم كان يرمي على أجهزة بلا بصمة → محاط بـ try/catch (يسقط إلى "غير مدعوم").

## 7) التطبيق (Flutter) قبل النشر (⚠️)
| البند | الإجراء |
|---|---|
| `BASE_URL` | مرّر عبر `--dart-define=BASE_URL=https://api.amialpay.com` (لا تترك الـ placeholder) |
| توقيع الإصدار | keystore (Android) + شهادة توزيع (iOS) — خارج Git |
| Certificate pinning | املأ بصمة الشهادة (انظر §3) |
| ملاحظات `flutter analyze` المتبقية (info) | غير حاجبة — انظر §8 |

---

## 8) ملاحظات Flutter analyze المتبقّية (info فقط — غير حاجبة)
خُفِّضت من **308 → 42** ملاحظة (0 أخطاء، 0 تحذيرات). المتبقّي **معلوماتي بحت** ولا
يمثّل أخطاء، ومؤجَّل عمداً لأنه يتطلّب تعديلات تحتاج تحقّقاً بصرياً على شاشات تعمل:

- **22 × `use_build_context_synchronously`** — استخدام `context` بعد `await` داخل
  حوارات GetX **محميّة أصلاً بفحص `mounted`** (المحلّل يطلب الفحص على الكائن الدقيق).
  سلوكها صحيح؛ التعديل يحتاج تحقّقاً يدوياً على كل حوار (بعضها حوارات مالية).
- **15 × `deprecated_member_use` (Radio)** — `groupValue`/`onChanged` في `RadioListTile`
  أُهملت في **Flutter 3.32+** لصالح `RadioGroup`. الكود **يعمل**؛ الترقية تغيير بنيوي
  يُفضَّل تنفيذه مع اختبار واجهة.
- **3 × widget طرف-ثالث (showcase)** — `use_key`/`overridden_fields` في ودجت مستورد
  يتعمّد تجاوز `key`؛ تركه أسلم.
- **1 × `share` (SharePlus)** — استدعاء مُهمَل واحد؛ ترقية API بسيطة عند الرغبة.

> لتشغيل التحليل: `cd 02_flutter_app && flutter analyze` — لن ترى أي **error** أو **warning**.

---

## خلاصة الحالة
- **الكود**: جاهز عالي الجودة (خلفية مُثبَتة 615 اختبار + تطبيق نظيف).
- **ما يفصلك عن الإطلاق**: إعداد **بيئة الإنتاج** (مفاتيح حيّة، HTTPS، نسخ احتياطي،
  مراقبة، رفع `max_connections`) — وهي بنود تشغيلية لا برمجية.
