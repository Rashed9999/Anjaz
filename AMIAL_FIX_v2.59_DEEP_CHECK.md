# الفحص العميق لـ v2.59 وإصلاحه — التقرير النهائي

فحص فعلي بالتشغيل على MariaDB 10.11 + Laravel 12.62 + PHP 8.4.

## الحالة: من «لا تُثبَّت ولا تُقلع» → **747 اختبار أخضر / 0 فشل**

| قبل الإصلاح | بعد الإصلاح |
|---|---|
| `composer install` يفشل | ✅ ينجح |
| `composer dump-autoload` ينهار (خطأ فادح) | ✅ نظيف |
| `php artisan` لا يُقلع | ✅ يُقلع (Laravel 12.62) |
| `route:cache` (نشر الإنتاج) ينهار | ✅ ينجح |
| كل واجهات التاجر/الكاشير معطّلة (500) | ✅ تعمل |
| كل تحويل مالي ينهار (SEND_MONEY غير معرّف) | ✅ يعمل |
| بوت واتساب: 500 على كل ويبهوك | ✅ يعمل + مُؤمَّن |
| الاختبارات: **36 فشل** (وأغلبها لا يُقلع) | ✅ **747 نجاح / 0 فشل** |

---

## 🔴 أخطاء حرجة أُصلحت (كانت تمنع التثبيت/الإقلاع/النشر)

حذف بوّابات الدفع تُرك **نصف منجز** — أُكمِل الآن:

1. **`composer.json > autoload.files`** كان يطلب 4 ملفّات محذوفة
   (Transaction/Constant/PaymentResponse/PaymentSuccess.php) → خطأ فادح عند كل إقلاع.
   أُزيلت المراجع الميتة.
2. **`composer.lock` غير متزامن** + مكتبة QR غائبة عنه → `composer install` يرفض،
   وكان سيعيد تثبيت stripe/razorpay/paystack «المحذوفة». أُعيد توليد الـ lock،
   وأُزيلت SDKs الميتة نهائياً (iyzico/mercadopago/xendit + مستودع phonepe).
3. **`app/Lib/Constant.php` حُذف بالخطأ** — وهو ملفّ **الثوابت الأساسية** (SEND_MONEY،
   CASH_OUT، أنواع المستخدمين…) الذي يعتمد عليه `TransactionTrait` القلبي. حذفه كان
   **يُنهي كل عملية مالية**. أُعيد (بقيمه الأصلية) وأُعيد ربطه في autoload.
4. **`WhatsappWebhookController`** يكتب `extends Controller` بلا استيراده →
   **500 على كل ويبهوك واتساب**. أُضيف الاستيراد.
5. **مسارات ميتة** إلى `PaymentOrderController` المحذوف في `routes/api/v1/api.php` → أُزيلت.
6. **تعارض رؤية دوال** بعد إعادة الهيكلة لـ `AmialApiController`: 5 متحكّمات
   (Branch/Cashier/FuelStation/Merchant/Rbac) تُعرّف `resolveMerchant` بـ `private`
   بتوقيع مختلف عن الأب `protected` → **خطأ فادح يُعطّل كل واجهات التاجر/الكاشير**
   و`route:cache`. أُعيدت تسميتها `resolveMerchantPos`.
7. **أسماء مسارات مكرّرة** (blog/faq/pages/helpTopic/linked-website) → **`route:cache`
   ينهار** (يكسر نشر الإنتاج الذي يشغّله). أُزيلت المكرّرات وسُمّيت المسارات الناقصة.

---

## 🟠/🟡 ثغرات أمنية ومالية أُصلحت (بوت واتساب)

8. **صرف مزدوج (double-spend):** استهلاك رمز PIN غير ذرّي + `PaymentRequestService::pay`
   يفحص الحالة خارج المعاملة → نقرتان متزامنتان تخصمان مرّتين.
   **الإصلاح:** قفل ذرّي (`Cache::lock`) على الرمز + إعادة فحص الحالة تحت
   `lockForUpdate` داخل المعاملة.
9. **تحقّق التوقيع كان يفشل «مفتوحاً»** خارج الإنتاج + يقرأ `env()` وقت الطلب
   (يُعطَّل بعد `config:cache`). نُقِل لـ `config/services.php` وصار **fail-closed** +
   `hash_equals`.
10. **DoS غير مُصادَق:** محمّل الوسائط بلا حدّ حجم/نوع، وفاكّ QR بلا حماية «قنبلة صور».
    أُضيف: سقف 3MB + تحقّق `image/*` + فحص أبعاد قبل GD + بوّابة «مربوط؟» قبل التحميل.
11. **لا idempotency لإعادات Meta** → أُضيف إزالة تكرار بـ `message.id` (`Cache::add`).
12. **لا throttle** على `/wa/webhook` و`/wa/pin` (تخمين PIN) → أُضيف.
13. **معالجة الويبهوك** كانت `dispatch(closure)` تفشل بتسلسل الـ closure → صارت
    `app()->terminating()` (داخل العملية، بلا تسلسل).
14. **تعارض IntentParser:** «cancel» يُصنَّف REJECT بدل CANCEL، ورابط `/req/CODE`
    يبتلعه استخراج المبلغ. أُصلح الترتيب والتعارض.

---

## 🛠️ إصلاحات DevOps

15. **امتداد Redis مفقود** من صورة الإنتاج بينما `.env.production` يستخدمه للكاش/الطابور/
    الجلسة → التطبيق يُقلع ثم يفشل أوّل عملية. أُضيف `pecl install redis`.
16. **FPM `clear_env=no`** — بلا هذا تضيع اعتمادات البيئة للـ workers بعد `config:cache`.
17. **مفاتيح Passport:** أُستُثنيت من الصورة (`.dockerignore`) وتُولَّد في `entrypoint.prod.sh`
    (كانت إمّا تُخبز في الصورة = تزوير الرموز، أو تغيب = فشل المصادقة).
18. **عمّال الطابور/المُجدول** يعملون الآن `user=www-data` (لا root).
19. **`backup.sh`** يشمل `storage/app` (وثائق KYC/الإيصالات) لا القاعدة فقط.
20. **`deploy.sh`:** rollback حقيقي (وسم الصورة السابقة واستعادتها عند فشل الفحص) +
    `set -euo pipefail` + إيقاف النشر إن لم تجهز القاعدة.

---

## ملاحظات
- **الاختبارات الـ17 "risky"** ليست فشلاً — تحذيرات Mockery (`shouldReceive` بلا تأكيد
  صريح) في اختبارات واتساب؛ سلوكها صحيح.
- اختبارات واتساب الأصلية كانت **معطوبة الكتابة** (أرقام غير متطابقة، وسيط constructor
  خاطئ، تعطيل دالّة private) — دليل أنها لم تُشغَّل قبل الشحن. أُصلحت لتختبر السلوك الفعلي.
- لم تُحذف بيانات ولا مُسّ منطق مالي؛ كل الإصلاحات دفاعية أو استعادة لِما حُذف خطأً.
</content>
