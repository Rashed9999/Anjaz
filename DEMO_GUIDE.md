# 🚀 أميال باي — دليل العرض على المستثمر

## المتطلّبات المسبقة (فقط هذه 3)

| المتطلّب | التثبيت |
|---|---|
| **Docker Desktop** | https://www.docker.com/products/docker-desktop |
| **Flutter SDK** | https://flutter.dev/docs/get-started/install (لبناء APK) |
| **هاتف أندرويد** | يُفعَّل "تثبيت من مصادر غير معروفة" في الإعدادات |

> اللابتوب والهاتف يجب أن يكونا على **نفس شبكة واي فاي** دائماً.

---

## ⚡ التشغيل السريع (قبل يوم العرض)

### الخطوة 1: تشغيل Backend

```bash
cd 01_backend

# تشغيل (أوّل مرّة: يأخذ 5 دقائق للبناء، بعدها 30 ثانية)
chmod +x demo.sh
./demo.sh start
```

**عند الانتهاء سترى:**
```
╔══════════════════════════════════════════════════════╗
║          الديمو جاهز! ✅                              ║
║  API URL (هاتف): http://192.168.1.X:8000             ║
╚══════════════════════════════════════════════════════╝
```

### الخطوة 2: بناء وتثبيت APK

```bash
cd ../02_flutter_app

# يكتشف IP لابتوبك تلقائياً ويبني APK مرتبطاً به
bash ../01_backend/build-apk.sh

# تثبيت على الهاتف (عبر USB)
adb install build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
```

### الخطوة 3: تفعيل واتساب (اختياري للعرض)

```bash
# 1. احصل على Token مجاني من: https://dashboard.ngrok.com
#    (إنشاء حساب مجاني → My Authtoken)

# 2. أضف Token في .env.demo:
echo "NGROK_AUTHTOKEN=your_token_here" >> 01_backend/.env.demo

# 3. شغِّل مع واتساب:
./demo.sh start-wa

# 4. اعرض رابط Webhook:
./demo.sh ngrok-url
# مثال: https://abc123.ngrok-free.app/wa/webhook

# 5. في Meta Developer Portal → WhatsApp → Configuration → Webhook:
#    Callback URL: https://abc123.ngrok-free.app/wa/webhook
#    Verify Token: amyal_webhook_2026
#    Subscribe: messages ✓
```

---

## 📋 سيناريو العرض الكامل (20 دقيقة)

### الدقيقة 1-3: الافتتاح

```
"أميال باي هي منصّة محفظة رقمية مصمَّمة خصيصاً للسوق اليمني.
تعمل عبر نموذج الوكلاء — شركات الصرافة المنتشرة في كل مدينة —
بدل الاعتماد على بنية بنكية تقليدية."
```

**افتح التطبيق على هاتفك → أرِه للمستثمر**

---

### الدقيقة 3-7: العميل العادي

**في التطبيق (تسجيل دخول عميل 1):**
```
الهاتف: 777100001
كلمة السر: Pass@2026
```

1. **الرصيد**: افتح الشاشة الرئيسية ← 50,000 ريال
2. **التحويل**:
   - اضغط "تحويل"
   - أدخل 777100002 (عميل 2)
   - أدخل 5,000 ريال
   - PIN: 1234
   - انظر: رصيد عميل 1 أصبح 45,000 ريال
3. **كشف الحساب**: يظهر التحويل فوراً

---

### الدقيقة 7-10: الوكيل (شحن المحفظة)

**في التطبيق (تسجيل دخول وكيل):**
```
الهاتف: 777300001
كلمة السر: Pass@2026
```

1. **رصيد الوكيل**: 500,000 ريال
2. **شحن محفظة عميل** → إدخال 777100001 → 10,000 ريال

```
"هكذا يشحن الوكيل محفظة أيّ مستخدم نقداً
 — نفس دور شركة الصرافة لكن رقمياً"
```

---

### الدقيقة 10-14: التاجر

**في التطبيق (تسجيل دخول تاجر):**
```
الهاتف: 777200001
كلمة السر: Pass@2026
```

1. **إنشاء فاتورة** → 15,000 ريال
2. **يُولَّد QR** → صوِّره بهاتف المستثمر
3. **المستثمر يمسح QR** → يرى الفاتورة → يؤكّد الدفع

```
"التاجر لا يحتاج POS بنكي مكلف
 — فاتورته رقمية وتُدفَع فوراً"
```

---

### الدقيقة 14-17: واتساب (إن كان مُفعَّلاً)

**على هاتف المستثمر → واتساب → رقم أميال:**

```
المستثمر يكتب: مرحبا
الردّ: قائمة الخدمات

يكتب: رصيدي
الردّ: رصيده الحالي

يكتب: حول 5000 لـ 777100002
الردّ: تأكيد مع رابط PIN
```

```
"حتى من لا يثبّت التطبيق يمكنه استخدام أميال باي
 — عبر واتساب الذي يعرفه الجميع"
```

---

### الدقيقة 17-20: الخلاصة والأرقام

**بيانات تتحدّث عن نفسها:**

| المؤشّر | القيمة |
|---|---|
| وقت التحويل | < 3 ثوانٍ |
| يعمل على أيّ هاتف | ✅ |
| بدون حساب بنكي | ✅ |
| يدعم واتساب | ✅ |
| جاهز للـ USSD مستقبلاً | ✅ |
| الوكلاء الشركاء | شركات صرافة موجودة فعلاً |

---

## 🔧 أوامر مفيدة أثناء العرض

```bash
# لو توقّف شيء
./demo.sh logs

# لو احتجت IP مجدّداً
./demo.sh ip

# لو احتجت رابط ngrok مجدّداً
./demo.sh ngrok-url

# إعادة تشغيل سريعة
./demo.sh stop && ./demo.sh start
```

---

## ❓ أسئلة المستثمر المتوقّعة

**"كيف تربح؟"**
```
رسوم بسيطة على كل معاملة (0.5-1%) تذهب لـ AMIAL_REVENUE_WALLET
— كل ريال مرّ عبر المنصّة يُسجَّل محاسبياً بـ Double-Entry Ledger
```

**"كيف يشحن المستخدم محفظته؟"**
```
عبر الوكيل (صرّاف محلّي) يدفع نقداً ويحصل على رصيد رقمي
— نفس ما تفعله شركات الصرافة، لكن ببنية تحتية مركزية وقابلة للتوسّع
```

**"هل هو مرخَّص؟"**
```
نحن في مرحلة Pilot مع شركاء وكلاء محدودين. مسار الترخيص
من البنك المركزي اليمني يحتاج شراكة مع بنك معتمد وهو جزء
من خطّة السنة الأولى
```

**"ما الفرق عن المنافسين؟"**
```
المنافسون يفترضون هاتفاً ذكياً وإنترنتاً جيّداً.
أميال باي يعمل على واتساب 2G + قابل للـ USSD مستقبلاً
— وصول لمن لا يصلهم غيرنا
```

---

## 🛑 قبل العرض بساعة — Checklist

- [ ] `./demo.sh start` يعمل بدون أخطاء
- [ ] التطبيق مثبَّت على الهاتف ويفتح
- [ ] تسجيل دخول عميل 1 (777100001) يعمل
- [ ] التحويل بين عميل 1 و2 يعمل
- [ ] واتساب Bot يردّ (إن كان مُفعَّلاً)
- [ ] شاحن اللابتوب معك ✓
- [ ] الهاتف شحن 100% ✓
- [ ] نسخة احتياطية على USB: `./demo.sh reset && ./demo.sh start`

---

## 🔁 إعادة الضبط الكاملة

```bash
./demo.sh reset    # يحذف كل البيانات
./demo.sh start    # يُنشئ بيانات تجريبية جديدة نظيفة
```

---

## 📚 للانتقال من الديمو إلى نشر إنتاجي حقيقي

راجع `README_DEVOPS.md` في جذر المشروع — يوثّق:
- `Dockerfile.prod` + `docker-compose.prod.yml` (مع Redis، بلا بيانات تجريبية)
- 3 إصلاحات DevOps حرجة أُجريت (TrustProxies، .gitignore، Health Check)
- سكريبتات النسخ الاحتياطي/الاستعادة/النشر الآمن
- قائمة تحقّق أمنية كاملة قبل أوّل نشر حقيقي

