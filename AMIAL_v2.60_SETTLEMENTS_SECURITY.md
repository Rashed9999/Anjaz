# تعزيز محرك التسويات + بوت واتساب (v2.60) — احترافية وأمان

بناءً على طلب تطوير الميزتين الجديدتين (**محرك التسويات** و**بوت واتساب**) لتكونا
أكثر احترافية وأماناً. كل الإصلاحات أدناه مُثبَتة باختبارات تعمل فعلياً على
MariaDB 10.11 / PHP 8.4 / Laravel 12.62.

## الحصيلة: **753 اختبار أخضر / 0 فشل** (17 «risky» = تحذيرات Mockery حميدة)

أُضيفت **6 اختبارات أمان جديدة** تُثبت الحواجز (وكيل محجوب، لا اعتماد ذاتي،
لا اعتماد مزدوج، سقف المبلغ).

---

## 🔴 C1 — بوّابة الأدمن كانت مقلوبة (الأخطر)

كان فحص «هل المستخدم أدمن؟» في عدّة مواضع يقارن `type === 1`. لكن حسب
`app/Lib/Constant.php`:

```
ADMIN_TYPE = 0    AGENT_TYPE = 1    CUSTOMER_TYPE = 2    MERCHANT_TYPE = 3
```

أي أنّ **النوع 1 هو الوكيل، لا الأدمن**. النتيجة: كان **كل وكيل يُعامَل كأدمن** —
يعتمد التسويات، يصل لإعدادات النظام، يُعفى من وضع الصيانة، ويظهر في «إشعارات الأدمن».
والأسوأ: الأدمن الحقيقي (النوع 0) كان **محجوباً** عن مساراته.

**المواضع المُصلَحة (8):**
- `AmialApiController::isAdmin` (الأساس الذي يرثه AdminSettlementController وغيره)
- `AdminPanelController` / `AccessController` / `WhatsappAdminController` / `AdminOpsController`
- `AmialMaintenanceMode` (إعفاء الصيانة)
- `HealthController::full` (فحص الصحّة للأدمن فقط)
- `CheckExpiringSubscriptionsJob` (digest الأدمن اليومي)
- `UniversalSettlementService` (بوّابة المدير المالي + وسم دور الفاعل في التدقيق)

المعيار الموحّد الآن:
```php
(int) ($user->type ?? -1) === 0
    || in_array($user->role ?? null, ['admin', 'super_admin'], true)
```

---

## 🔴 C2 — فصل المهام في الاعتماد (Separation of Duties)

من ينشئ التسوية كان يعتمدها بنفسه. أُضيف منع صريح: **المُنشئ ≠ المعتمِد**.
```php
if ((int) $settlement->created_by === (int) $actor->id) {
    throw new RuntimeException('لا يمكن اعتماد تسوية أنشأتها بنفسك (فصل المهام)');
}
```

## 🔴 C3 — سباق الاعتماد/التنفيذ المزدوج (Double-Processing)

كان `assertCanTransition` يفحص الحالة على نسخة **قديمة** خارج المعاملة، وصفّ
التسوية **لا يُقفَل** داخلها. طلبان متزامنان يعتمدان/ينفّذان التسوية مرّتين → **قيدان
محاسبيان مزدوجان**. الحلّ: دالّة `lockForTransition` تعيد تحميل التسوية بـ
`lockForUpdate` ثم تعيد فحص الانتقال على الحالة الطازجة، مطبَّقة على كل الانتقالات
(submit/approve/reject/startProcessing/complete/cancel). الطلب الثاني ينتظر القفل ثم
يفشل بأمان.

## 🔴 C4 — سباق الاعتماد المزدوج في شحن الوكيل

نفس المشكلة في `AgentNetworkService::approveSettlement`: فحص `status !== 'pending'`
خارج المعاملة، وإضافة الرصيد (`guard->credit`) غير idempotent. طلبان متزامنان
يضيفان الرصيد **مرّتين**. الحلّ: قفل صفّي + إعادة فحص الحالة داخل المعاملة.

## 🟠 H1 — تفويض الاعتماد

- تنفيذ/إكمال التسوية (`startProcessing`/`complete`) يمسّ الدفتر المحاسبي لكنه لم يكن
  يتحقّق من صلاحية المدير المالي. أُضيف `assertIsFinanceManager`.
- اعتماد شحن الوكيل لم يكن يتحقّق من هوية المعتمِد إطلاقاً. أُضيف `assertCanApprove`:
  **الأدمن** أو **الموزّع المُسوّى معه تحديداً** فقط.

## 🟠 H2 — حدود مالية

- أُزيل `allowNegative: true` من قيد شحن الوكيل (القيد يزيد الاحتياطي ولا يُنقصه؛
  إبقاؤه كان يسمح بخلق رصيد بلا غطاء نظرياً).
- أُضيف **سقف عقلاني لمبلغ الشحن** (`config('amial.agent.max_topup')`، افتراضي
  100 مليون) يمنع الأخطاء الجسيمة/الإساءة.

---

## الاختبارات الجديدة (تُثبت الحواجز)

| الاختبار | يُثبت |
|---|---|
| `agent_cannot_approve_settlement` | الوكيل (type=1) محجوب عن اعتماد التسوية (C1) |
| `creator_cannot_approve_own_settlement` | فصل المهام (C2) |
| `cannot_approve_the_same_settlement_twice` | لا اعتماد مزدوج (C3) |
| `agent_cannot_start_processing` | التنفيذ يتطلّب مديراً مالياً (H1) |
| `non_admin_cannot_approve_topup` | عميل/دخيل محجوب عن اعتماد الشحن (H1) |
| `topup_above_max_is_rejected` | سقف مبلغ الشحن (H2) |

كما حُدِّثت الحمولات الاختبارية القديمة التي كانت تُنشئ «أدمن» بالنوع 1 (وهو وكيل)
إلى النوع 0 + دور `super_admin` — لأنها بعد إصلاح C1 لم تعد تُمثّل أدمن.

---

## بوت واتساب (مكتمل من الفحص السابق)

تحقّق توقيع fail-closed + hash_equals، idempotency لإعادات Meta، أقفال ذرّية ضدّ
الصرف المزدوج على رمز PIN وطلبات الدفع، حدود DoS على الوسائط/فاكّ QR، وthrottle على
الويبهوك ومسار PIN. (تفاصيلها في `AMIAL_FIX_v2.59_DEEP_CHECK.md`.)
